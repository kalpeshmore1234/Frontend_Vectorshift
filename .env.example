# Copy to .env.local for local overrides (not committed).
# On Netlify: Site settings → Environment variables → add REACT_APP_API_URL

# Local dev (default in code if unset):
# REACT_APP_API_URL=http://localhost:8000

# Production (Netlify → Site settings → Environment variables → Build):
# REACT_APP_API_URL=https://backend-vectorshift-eex1.onrender.com
